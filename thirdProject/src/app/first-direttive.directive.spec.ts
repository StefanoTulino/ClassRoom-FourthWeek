import { FirstDirettiveDirective } from './first-direttive.directive';

describe('FirstDirettiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstDirettiveDirective();
    expect(directive).toBeTruthy();
  });
});
